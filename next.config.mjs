/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add external domains here if you use them for images
    remotePatterns: []
  }
};

export default nextConfig;

